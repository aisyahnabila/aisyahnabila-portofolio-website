# Visitor counter (api/visits.ts) — Upstash Redis REST credentials.
# Create a free database at https://console.upstash.com, then copy its
# REST URL and token here (or set them as Environment Variables in the
# Vercel project dashboard for production).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
